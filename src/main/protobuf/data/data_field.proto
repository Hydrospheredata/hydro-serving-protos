syntax = "proto3";

package hydroserving;

import "tf/tensor.proto";

message DataField {
    message Dict {
        map<string, DataField> subfields = 1;
    }
    oneof data_field {
        hydroserving.tensorflow.TensorProto tensor = 1;
        Dict dict = 2;
    }
}