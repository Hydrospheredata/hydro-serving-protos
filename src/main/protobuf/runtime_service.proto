syntax = "proto3";

package hydroserving;

import "data/inference_request.proto";
import "data/inference_result.proto";

service InferenceService {
    rpc Infer(data.InferenceRequest) returns (data.InferenceResult) {}
}

